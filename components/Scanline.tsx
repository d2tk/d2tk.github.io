import React from 'react';

const Scanline: React.FC = () => {
    return (
        <div className="fixed inset-0 w-full h-full scanline z-40 pointer-events-none"></div>
    );
};

export default Scanline;